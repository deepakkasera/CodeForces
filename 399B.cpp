#include <bits/stdc++.h>
#define ll long long 
using namespace std;
int main(int argc, char const *argv[])
{
	ll n,l,r;
	cin >> n >> l >> r;
	int length = pow(2,ceil(log2(n))) - 1;
	//cout << length << endl;

	
	return 0;
}